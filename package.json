{
  "name": "gdpi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "socket.io-client": "^4.4.1"
  },
  "scripts": {
    "sync": "rsync -va --exclude .idea ../gdpi pi@piet.local:.",
    "syncrus": "rsync -va --exclude .idea ../gdpi/ pi@gdpi-rus.local:./gdpi-2/"
  }
}
